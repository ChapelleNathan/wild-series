{% extends 'base.html.twig' %}

{% block title %}{{ 'editSeason'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'editSeason'|trans }}</h1>

    {{ include('season/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('season_index') }}">{{ 'back'|trans }}</a>

    {{ include('season/_delete_form.html.twig') }}
{% endblock %}

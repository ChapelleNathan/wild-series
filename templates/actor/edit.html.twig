{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - {{ 'editActor'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'editActor'|trans }}</h1>

    {{ include('actor/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary" href="{{ path('actor_index') }}">{{ 'back'|trans }}</a>

    {{ include('actor/_delete_form.html.twig') }}
{% endblock %}
